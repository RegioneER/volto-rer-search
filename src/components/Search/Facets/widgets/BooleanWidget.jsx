import React from 'react';
import { SelectWidget } from 'volto-rer-search/components/Search';

const BooleanWidget = (props) => {
  return <SelectWidget {...props} />;
};

export default BooleanWidget;
